DATABASE_URL=postgresql://movie:movie@localhost:5432/movie_reservation
SECRET_KEY=change-me-to-a-long-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=10080
# Optional: if set, `python -m app.seed` fetches each movie poster from TMDB instead of bundled JSON.
# TMDB_API_KEY=
